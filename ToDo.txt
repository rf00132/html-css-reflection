ToDo List:

IE fixes

  - Netmatters logo top left in the header 
    doesnt shrink at mobile level

  - grayscale hover doesnt work so need to 
    change it up so it replaces the image instead

Seems fine:

Mobile Level site (excluding carousel)



done

